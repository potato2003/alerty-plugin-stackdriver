require "spec_helper"

RSpec.describe Alerty::Plugin::Stackdriver do
  it "has a version number" do
    expect(Alerty::Plugin::Stackdriver::VERSION).not_to be nil
  end

  it "does something useful" do
    expect(false).to eq(true)
  end
end
